#ifndef LCB_MODEL_DEFAULTS_H
#define LCB_MODEL_DEFAULTS_H
#include "library_win.h"
/* Local data only. thinking is a saved user choice, or -1 for the default. */
void model_defaults(const ModelInfo *model, int thinking, Generation *settings);
#endif
